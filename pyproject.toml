[project]
name = "career-vault-resume"
version = "0.1.0"
description = "Local-first professional identity and career memory vault skill."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
