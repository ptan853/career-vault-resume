[project]
name = "career-timeline"
version = "0.1.0"
description = "Local-first professional timeline and identity memory skill."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
